class PagesController < ApplicationController

	def index
	end

	def admin_index
		@password = "potatoes"
	end

	def admin_create_prompt
	end

	def admin_edit_prompt
		@prompt = Prompt.find(params[:id])
	end

	def admin_delete_prompt
		@prompt = Prompt.find(params[:id])
	end

	def admin_create_limit
	end

	def admin_edit_limit
		@limit = Limit.find(params[:id])
	end

	def admin_delete_limit
		@limit = Limit.find(params[:id])
	end

end
